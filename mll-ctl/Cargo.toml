[package]
name = "mll-ctl"
version = "0.0.0"
edition = "2024"

[[bin]]
name = "mll"
path = "src/main.rs"

[dependencies]
mll-core = { path = "../mll-core" }

clap = { version = "4", features = ["cargo"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1"

interprocess = "2"
