[package]
name = "mll-ctl"
version = "0.0.0"
edition = "2024"

[[bin]]
name = "mll"
path = "src/main.rs"

[dependencies]
mll-core = { path = "../mll-core" }

clap = { version = "4", features = ["cargo"] }

interprocess = "2"
serde_json = "1"
